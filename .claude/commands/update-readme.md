Analyze the current state of the entire project — folder structure, 
installed dependencies, configured APIs, environment variables, 
and implemented features. Then update the CLAUDE.md file at the 
root of the project to accurately reflect the current state of 
the codebase, tech stack, and any changes made since the last update.
Keep the existing format but replace outdated information.